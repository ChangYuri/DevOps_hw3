[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flake8>=7.3.0",
    "flask>=3.1.2",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
]
